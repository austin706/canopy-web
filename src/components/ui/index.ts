/**
 * Canopy Web UI primitives (shipped Wave B · systemic wins #5–#8).
 *
 * Import from `@/components/ui` to get the shared building blocks used by
 * every DD-* dashboard card, commerce surface, and admin page going forward.
 */
export { Card } from './Card';
export type { CardVariant, CardPadding } from './Card';
export { EmptyState } from './EmptyState';
export { Skeleton, SkeletonCard, SkeletonList } from './Skeleton';
export { ConfirmProvider, useConfirm } from './ConfirmDialog';
export type { ConfirmOptions } from './ConfirmDialog';
export { DashboardSection } from './DashboardSection';
